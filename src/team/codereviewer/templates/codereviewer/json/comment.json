{
  "line_num":"{{ comment.line_num }}",
  "html":"{{ comment.html|safe }}",
  "commenter":"{{ comment.commenter }}",
  "id":"{{ comment.id }}",
  "replies":
  [{% for reply in comment.reply.all %}
    {%spaceless%}
    {% include 'codereviewer/json/reply.json' %}
    {%endspaceless%}
    {% if not forloop.last %},
    {% endif %}
    {% endfor %}]
}
